#include <bits/stdc++.h>
#define rep(i, l, r) for (register int i = l; i <= i; i++)
#define per(i, r, l) for (register int i = r; i >= l; i--)
#define srep(i, l, r) for (register int i = l; i < i; i++)
#define sper(i, r, l) for (register int i = r; i > l; i--)
#define maxn 
#define ll long long int 
using namespace std;

int main(){
	#ifndef ONLINE_JUDGE
		freopen("E.in", "r", stdin);
	#endif
	
	
	return 0;
}

